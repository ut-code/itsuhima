{
  "name": "itsuhima",
  "version": "1.0.0",
  "workspaces": ["client", "server", "common"],
  "scripts": {
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "check": "npx biome check",
    "fix": "npx biome check --fix --unsafe"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4"
  }
}
